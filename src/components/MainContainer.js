import React from "react";

const MainContainer = () => {
  return (
    <div className="MainContainer">I am the main container</div> //good idea for the main thing that is being returned from each component to get a div of the name of the component spelled the exact same way.
  );
};
export default MainContainer;

//gonna be where all the action happens probably (like the main thing we are looking at.)
